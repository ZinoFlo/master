# Sentinel Security Journal

## Critical Security Learnings
- **Vulnerability Patterns**: Use 'textContent' over 'innerHTML' to prevent DOM XSS.
- **Environment Constraints**: 'docker buildx build' fails due to 'overlayfs' mount errors in the sandbox.
- **Best Practices**: Implement CSP in 'index.html' to restrict script/style sources.

## Security Fixes
- Initialized 'SECURITY.md' with private reporting instructions.
- Configured 'dependabot.yml' for npm and docker updates.
